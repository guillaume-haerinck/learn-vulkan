#pragma once

#include <vulkan/vulkan.h>

class VertexInputDescription {
public:
    VertexInputDescription(/* args */);
    ~VertexInputDescription();
};

class VertexBuffer {
public:
    VertexBuffer();
    ~VertexBuffer();

private:
};
